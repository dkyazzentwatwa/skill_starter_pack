interface:
  display_name: "Safe Web Research"
  short_description: "Research with sources, caveats, and next steps"
  default_prompt: "Use $safe-web-research to research this question and separate facts from assumptions."
policy:
  allow_implicit_invocation: true
