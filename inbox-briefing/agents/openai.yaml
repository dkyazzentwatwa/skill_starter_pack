interface:
  display_name: "Inbox Briefing"
  short_description: "Summarize Gmail and Calendar into a daily brief"
  default_prompt: "Use $inbox-briefing to summarize my inbox and calendar into a daily briefing."
policy:
  allow_implicit_invocation: true
