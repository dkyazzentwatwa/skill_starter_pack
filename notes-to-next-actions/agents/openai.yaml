interface:
  display_name: "Notes To Next Actions"
  short_description: "Turn messy notes into decisions and tasks"
  default_prompt: "Use $notes-to-next-actions to turn these notes into a clear action list."
policy:
  allow_implicit_invocation: true
