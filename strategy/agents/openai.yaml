interface:
  display_name: "Strategy"
  short_description: "Rank business options by fit and tradeoff"
  default_prompt: "Use $strategy to help me choose what to focus on next."
policy:
  allow_implicit_invocation: true
